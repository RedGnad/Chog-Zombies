using UnityEngine;

namespace ChogZombies.Player
{
    /// <summary>
    /// Marqueur vide posé sur les clones visuels de soldats
    /// pour différencier leurs composants (ex: TrailRenderer) de ceux du soldat principal.
    /// </summary>
    public class SoldierVisualMarker : MonoBehaviour
    {
    }
}
